APP_NAME="Property Description Generator"
APP_ENV=local
APP_KEY=base64:CtE3XlF8zFJYzKm+t9XP20KL/Qt4fiBw5y+4BOfEvhU=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_LEVEL=debug

## Use SQLite for quick local development. To use MySQL, replace with the MySQL settings below.
DB_CONNECTION=sqlite
#DB_CONNECTION=mysql
#DB_HOST=127.0.0.1
#DB_PORT=3306
#DB_DATABASE=your_database_name
#DB_USERNAME=your_database_user
#DB_PASSWORD=your_database_password

# SQLite database file path (relative to project root)
DB_DATABASE=database/database.sqlite

BROADCAST_DRIVER=log
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MAIL_MAILER=smtp
MAIL_HOST=mail.example.com
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=hello@example.com
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

CACHE_DRIVER=file
APP_CLI_MEMORY_LIMIT=1G
APP_HTTP_MEMORY_LIMIT=1G

# Optional OpenAI configuration for AI-generated descriptions
# If not set the app will use a helpful offline fallback.
OPENAI_API_KEY=
# Optional: custom OpenAI-compatible endpoint (for local proxies or compat services)
OPENAI_API_URL=https://api.openai.com/v1/chat/completions
# Default model to use
OPENAI_MODEL=gpt-3.5-turbo

